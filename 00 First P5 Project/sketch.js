// Project Title
// Your Name
// Date
// One-line description (or more if needed)
// Extra for Experts:
// - describe what you did to take this project "above and beyond"

// interactive program

function setup() {
  //this happens exactly once, at the start
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  //happens over and over (60 fps)
  //background(220);
  circle(mouseX, mouseY, 30)
}
